#include <stdio.h>

int main() {
    int num;
    int soma = 0;

    while (soma <= 100) {
        printf("Digite um número: ");
        scanf("%d", &num);

        soma += num;
    }

    printf("Soma final: %d\n", soma);

    return 0;
}
